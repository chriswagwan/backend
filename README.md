# backend

## Environment variables

Set the following variables before starting the API:

- `MONGO_URI`
- `JWT_SECRET`
- `CLIENT_URL`
- `CLOUDINARY_CLOUD_NAME`
- `CLOUDINARY_API_KEY`
- `CLOUDINARY_API_SECRET`

Uploaded project images and staff photos are stored in Cloudinary and persisted in MongoDB as full asset URLs.
